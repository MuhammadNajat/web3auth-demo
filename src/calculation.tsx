function add(number1: number, number2: number) {
    return number1 + number2;
}

function subtract(number1: number, number2: number) {
    return number1 - number2;
}

function square(number: number) {
    return number * number;
}

export default {add, subtract, square};